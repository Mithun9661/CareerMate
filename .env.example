# Optional server-owned key; otherwise enter your key with Connect Gemini in the UI.
GEMINI_API_KEY=
# Optional supported model ID. If empty, the backend discovers an available Flash model.
GEMINI_MODEL=
